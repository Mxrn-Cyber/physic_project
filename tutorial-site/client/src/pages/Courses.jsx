// Not routed / not linked from the nav -- there's no public "Courses"
// browsing page in this app right now (removed per request). The
// underlying course grouping still exists (videos/books have a `course`
// field, and GET /api/courses on the backend still works), this page just
// isn't wired up to it.
//
// I can't delete files on your computer from here, so this is left as a
// comment instead of a working page. Safe to delete by hand
// (src/pages/Courses.jsx), or re-wire it later -- see git history / the
// earlier version of this file for a working starting point.
