// This file is unused and not routed in App.jsx. It used to be linked from
// a "View plans" button in Lesson.jsx (CourseDetail), but this app doesn't
// actually have subscription plans -- every video and book is bought
// individually (see VideoDetail.jsx / BookDetail.jsx / PaymentModal.jsx).
// That button now sends people to the item's own page instead, so this
// page has nothing left to do.
//
// I can't delete files on your computer from here, so this is left as a
// comment instead of the original empty file. Safe to delete by hand
// (src/pages/Pricing.jsx) unless you plan to add real subscription plans
// later, in which case build the page here and add a route + nav link.
