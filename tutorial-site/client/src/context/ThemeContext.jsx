// This file is unused -- nothing in the app imports ThemeContext (grep the
// src/ folder to confirm). Dark mode is actually handled by
// DarkModeContext.jsx, and the unrelated `ThemeProvider` wired up in
// main.jsx comes from the @material-tailwind/react library, not this file.
// This looks like an early leftover from before DarkModeContext existed.
//
// I can't delete files on your computer from here, so this is left as a
// comment instead of the original empty file. Safe to delete by hand
// (src/context/ThemeContext.jsx) unless you have a reason to build a real
// theme system here later.
