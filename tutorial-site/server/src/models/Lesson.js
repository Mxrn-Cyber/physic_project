// This model is currently unused -- no route imports it (grep the routes/
// folder to confirm). It looks like a leftover placeholder from early
// planning rather than a finished feature.
//
// I can't delete files on your computer from here, so this is left as a
// comment instead of the original empty file. If you don't have plans to
// break videos/books down into individual lessons, it's safe to delete
// src/models/Lesson.js by hand. If you do want that feature, build the
// schema out here (e.g. course/video/book ref, order, content) and wire it
// into routes/ + the Course response before using it.
