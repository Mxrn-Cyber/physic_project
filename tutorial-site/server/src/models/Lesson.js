// Deprecated: split into Video.js (video-only) and Book.js (PDF-only) so
// each can be added and priced independently in the admin panel. Unused,
// not imported anywhere -- safe to delete manually.
